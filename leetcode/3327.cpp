#include <iostream>
#include <vector>
#include <cmath>
using namespace std;

class Solution
{
public:
    struct treenode
    {
        int num;
        treenode *child;
        treenode *bro;
    };

    vector<bool> findAnswer(vector<int> &parent, string s)
    {
        int n = parent.size();
        queue for (int i = 0; i < n; i++)
        {
        }
    }
};